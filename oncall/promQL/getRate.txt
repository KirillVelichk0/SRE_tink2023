rate(oncallnotifier_message_blackhole_cnt[10m])

Скорость роста метрики за заданный интервал времени. Стандартное предназначение - получение именно частоты, например, http запросов. Пример:
rate(prometheus_http_requests_total[12m])

Данный запрос получит частоту http запросов (в секунду)
